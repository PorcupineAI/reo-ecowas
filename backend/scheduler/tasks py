from apscheduler.schedulers.background import BackgroundScheduler
import logging
logger = logging.getLogger(__name__)

scheduler = BackgroundScheduler()

def update_scores():
    logger.info("Updating suitability scores...")

def start_scheduler():
    scheduler.add_job(update_scores, 'interval', days=1)
    scheduler.start()
